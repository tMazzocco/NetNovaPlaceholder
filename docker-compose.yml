# Full Wazuh stack (manager + indexer + dashboard) + the Slack connector.
#
# FIRST RUN — generate TLS certs once:
#   docker compose -f generate-certs.yml run --rm generator
#
# Then launch:
#   SLACK_BOT_TOKEN=xoxb-... SLACK_APP_TOKEN=xapp-... docker compose up --build -d
#
# Dashboard UI:   https://localhost   (admin / SecretPassword)
# Alerts (file):  docker compose exec wazuh.manager tail -f /var/ossec/logs/alerts/alerts.json
#
# The connector writes NDJSON into the shared `slack-logs` volume; the manager
# tails it (see <localfile> in config/wazuh_cluster/wazuh_manager.conf) and runs
# the custom Slack rules/decoder mounted below.

services:
  wazuh.manager:
    image: wazuh/wazuh-manager:4.9.0
    hostname: wazuh.manager
    restart: always
    ulimits:
      memlock: { soft: -1, hard: -1 }
      nofile: { soft: 655360, hard: 655360 }
    ports:
      - "1514:1514"
      - "1515:1515"
      - "514:514/udp"
      # Host 55000 falls inside a Windows/WinNAT reserved port range (bind fails
      # with "forbidden by its access permissions"), so publish the manager API
      # on 55500 instead. Container port stays 55000; the dashboard reaches the
      # manager over the compose network and is unaffected by this remap.
      - "55500:55000"
    environment:
      - INDEXER_URL=https://wazuh.indexer:9200
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=SecretPassword
      - FILEBEAT_SSL_VERIFICATION_MODE=full
      - SSL_CERTIFICATE_AUTHORITIES=/etc/ssl/root-ca.pem
      - SSL_CERTIFICATE=/etc/ssl/filebeat.pem
      - SSL_KEY=/etc/ssl/filebeat.key
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
    volumes:
      - wazuh_api_configuration:/var/ossec/api/configuration
      - wazuh_etc:/var/ossec/etc
      - wazuh_logs:/var/ossec/logs
      - wazuh_queue:/var/ossec/queue
      - wazuh_var_multigroups:/var/ossec/var/multigroups
      - wazuh_integrations:/var/ossec/integrations
      - wazuh_active_response:/var/ossec/active-response/bin
      - wazuh_agentless:/var/ossec/agentless
      - wazuh_wodles:/var/ossec/wodles
      - filebeat_etc:/etc/filebeat
      - filebeat_var:/var/lib/filebeat
      - ./config/wazuh_indexer_ssl_certs/root-ca-manager.pem:/etc/ssl/root-ca.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.manager.pem:/etc/ssl/filebeat.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.manager-key.pem:/etc/ssl/filebeat.key
      - ./config/wazuh_cluster/wazuh_manager.conf:/wazuh-config-mount/etc/ossec.conf
      # Custom Slack rules + decoder + CDB lists. These MUST be mounted under
      # /wazuh-config-mount (not directly into /var/ossec/etc) — the entrypoint
      # copies them into place AFTER the first-run seeding step. Mounting them
      # straight into the wazuh_etc volume makes /var/ossec/etc non-empty before
      # init, which makes mount_permanent_data() skip seeding etc/shared/ar.conf,
      # so analysisd fails (1103) and the manager API never starts (dashboard 3099).
      - ./wazuh-rules/0500-slack-rules.xml:/wazuh-config-mount/etc/rules/0500-slack-rules.xml
      - ./wazuh-decoders/0501-slack-decoder.xml:/wazuh-config-mount/etc/decoders/0501-slack-decoder.xml
      # login-context CDB allow-lists (Gap 4 — rules 100091/100092)
      - ./wazuh-lists/slack-trusted-networks:/wazuh-config-mount/etc/lists/slack-trusted-networks
      - ./wazuh-lists/slack-allowed-countries:/wazuh-config-mount/etc/lists/slack-allowed-countries
      # shared NDJSON volume with the connector
      - slack-logs:/var/log/wazuh-slack

  wazuh.indexer:
    image: wazuh/wazuh-indexer:4.9.0
    hostname: wazuh.indexer
    restart: always
    ports:
      - "9200:9200"
    environment:
      - "OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g"
    ulimits:
      memlock: { soft: -1, hard: -1 }
      nofile: { soft: 65536, hard: 65536 }
    volumes:
      - wazuh-indexer-data:/var/lib/wazuh-indexer
      - ./config/wazuh_indexer_ssl_certs/root-ca.pem:/usr/share/wazuh-indexer/certs/root-ca.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.indexer-key.pem:/usr/share/wazuh-indexer/certs/wazuh.indexer-key.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.indexer.pem:/usr/share/wazuh-indexer/certs/wazuh.indexer.pem
      - ./config/wazuh_indexer_ssl_certs/admin.pem:/usr/share/wazuh-indexer/certs/admin.pem
      - ./config/wazuh_indexer_ssl_certs/admin-key.pem:/usr/share/wazuh-indexer/certs/admin-key.pem
      - ./config/wazuh_indexer/wazuh.indexer.yml:/usr/share/wazuh-indexer/opensearch.yml
      - ./config/wazuh_indexer/internal_users.yml:/usr/share/wazuh-indexer/opensearch-security/internal_users.yml

  wazuh.dashboard:
    image: wazuh/wazuh-dashboard:4.9.0
    hostname: wazuh.dashboard
    restart: always
    ports:
      - "443:5601"
    environment:
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=SecretPassword
      - WAZUH_API_URL=https://wazuh.manager
      - DASHBOARD_USERNAME=kibanaserver
      - DASHBOARD_PASSWORD=kibanaserver
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
    volumes:
      - ./config/wazuh_indexer_ssl_certs/wazuh.dashboard.pem:/usr/share/wazuh-dashboard/certs/wazuh.dashboard.pem
      - ./config/wazuh_indexer_ssl_certs/wazuh.dashboard-key.pem:/usr/share/wazuh-dashboard/certs/wazuh.dashboard-key.pem
      - ./config/wazuh_indexer_ssl_certs/root-ca.pem:/usr/share/wazuh-dashboard/certs/root-ca.pem
      - ./config/wazuh_dashboard/opensearch_dashboards.yml:/usr/share/wazuh-dashboard/config/opensearch_dashboards.yml
      - ./config/wazuh_dashboard/wazuh.yml:/usr/share/wazuh-dashboard/data/wazuh/config/wazuh.yml
    depends_on:
      - wazuh.indexer

  wazuh-slack:
    build: .
    hostname: wazuh.slack
    restart: unless-stopped
    depends_on:
      - wazuh.manager
    environment:
      SLACK_BOT_TOKEN: ${SLACK_BOT_TOKEN}
      SLACK_APP_TOKEN: ${SLACK_APP_TOKEN}
      SLACK_USER_TOKEN: ${SLACK_USER_TOKEN}
    command: ["--config", "/etc/wazuh-slack/wazuh-slack.docker.yaml"]
    ports:
      - "9183:9183"   # Prometheus /metrics
      - "9184:9184"   # /healthz
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9184/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    volumes:
      - ./config:/etc/wazuh-slack:ro
      - slack-logs:/var/log/wazuh-slack
      - slack-state:/var/lib/wazuh-slack

volumes:
  wazuh_api_configuration:
  wazuh_etc:
  wazuh_logs:
  wazuh_queue:
  wazuh_var_multigroups:
  wazuh_integrations:
  wazuh_active_response:
  wazuh_agentless:
  wazuh_wodles:
  filebeat_etc:
  filebeat_var:
  wazuh-indexer-data:
  slack-logs:
  slack-state:
