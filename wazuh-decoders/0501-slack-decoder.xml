<!--
  Slack source decoder for the wazuh-slack connector.
  Events arrive on the analysisd queue with location prefix `slack-<source>`
  and the NormalizedEvent JSON payload (top-level fields: @timestamp, slack.*,
  severity, raw). Wazuh's built-in JSON decoder handles field extraction;
  this file just attaches the `slack` decoder name and groups so rules can
  match on `<decoded_as>slack</decoded_as>`.

  NOTE: there are deliberately NO per-source child decoders. The rules
  differentiate sources via the JSON `slack.source` field (e.g.
  `<field name="slack.source">audit</field>`), not via the decoder name. A
  child decoder with <use_own_name>true</use_own_name> would override
  `decoded_as` to `slack-<source>` and break rule 100000's
  `<decoded_as>slack</decoded_as>` match.
-->
<decoder name="slack">
  <prematch>^{</prematch>
  <plugin_decoder>JSON_Decoder</plugin_decoder>
</decoder>
